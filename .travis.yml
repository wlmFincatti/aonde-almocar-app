language: node_js
node_js:
  - "12.8.0"
branches:
  only:
    - master
before_script:
  - npm install -g firebase-tools
  - npm install -g ionic
script:
  -ionic build 
after_success:
  - firebase deploy --token=${FIREBASE_TOKEN}
